import React from 'react';
import { Form } from 'antd';

const PostForm = () => {
  const form = Form.useForm();

  return (
    <Form
      form={form}
    >

    </Form>
  );
};

export default PostForm;
